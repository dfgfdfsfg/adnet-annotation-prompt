# ADNet Annotation Prompt

This repository provides the public text-annotation prompt assets used in the ADNet Pattern Recognition revision.

Files:

- `text_annotation_prompt_template.md`: prompt template for structured anomaly text annotation.
- `adnet_annotation_schema.json`: JSON schema for annotation records.
- `example_annotation_record.json`: example annotation record following the schema.
